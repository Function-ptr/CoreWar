/*
** EPITECH PROJECT, 2023
** parser.c
** File description:
** parse tokens and link them
*/
/*
 __  __        _                            ___            ___
|  \/  |      | |                          / _ \          / _ \.
| \  / | _ __ | |       ___   __ _   ___  | (_) |        | (_) |
| |\/| || '__|| |      / _ \ / _` | / _ \  > _ <          \__, |
| |  | || |   | |____ |  __/| (_| || (_) || (_) |           / /
|_|  |_||_|   |______| \___| \__, | \___/  \___/           /_/
                              __/ |               ______
                             |___/               |______|
*/
#include "parser.h"
#include <stdlib.h>
#include "my.h"

bool add_and_check_labels(token_t array tokens, labels_t array labels,
    uint32_t ptr array data, line_t array lines)
{
    uint32_t ptr curr_token = data[2], bytes_pos = *(data[3]);
    uint16_t line_nb = (uint16_t)*(data[1]);
    if (tokens[*curr_token].type == TOKEN_LABEL) {
        bool exists = add_label(labels, bytes_pos,
            tokens[*curr_token],
            line_nb);
        if (exists) {
            free(lines);
            clean_labels_struct(labels);
            return true;
        }
        *curr_token += 1;
    }
    return false;
}

bool update_line(token_t array tokens, uint32_t ptr array data,
    line_t array ptr lines, labels_t ptr labels)
{
    if (tokens[*data[2]].type == TOKEN_LABEL ||
    tokens[*data[2]].type == TOKEN_END || tokens[*data[2]].type == TOKEN_NEWLINE)
        return false;
    uint16_t line_nb = (uint16_t)*(data[1]), nb_lines = (uint16_t)*(data[0]);
    uint32_t ptr len_output = data[4], ptr bytes_pos = data[3];
    line_t l = link_line(tokens, data[2], line_nb);
    if (l.mnemonic == NULL) {
        if (*lines) free(*lines);
        clean_labels_struct(labels);
        return true;
    }
    line_t array tmp = realloc(*lines, sizeof(line_t) * (nb_lines + 1));
    if (!tmp) {
        if (*lines) free(*lines);
        clean_labels_struct(labels);
        return true;
    }
    tmp[*len_output] = l; *lines = tmp;
    *len_output += 1; *bytes_pos += l.bytes_size;
    return false;
}

line_t array parser(token_t array tokens, uint16_t nb_lines,
    uint16_t line_nb, uint32_t ptr len_output)
{
    uint32_t curr_token = 0, bytes_pos = 0,
    *data[5] = {&(uint32_t){nb_lines},
        &(uint32_t){line_nb}, &curr_token, &bytes_pos, len_output};
    line_t array lines = my_calloc(sizeof(line_t), 1, 0);
    if (!lines) return NULL;
    labels_t *labels = init_labels();
    while (tokens[curr_token].type != TOKEN_END) {
        if (tokens[curr_token].type == TOKEN_NEWLINE) {
            *data[1] = line_nb = line_nb + 1;
            curr_token += 1;
            continue;
        }
        if (tokens[curr_token].type == TOKEN_LABEL &&
        add_and_check_labels(tokens, labels, data, lines)) return NULL;
        if (update_line(tokens, data, &lines, labels)) return NULL;
    } if (replace_all_labels_refs(labels, lines, *len_output)) {
        free(lines); lines = NULL;
    }
    clean_labels_struct(labels);
    return lines;
}
/*
⠀⠀⠀⠀⠀⠀⠀⠀⢀⡴⠊⠉⠉⢉⠏⠻⣍⠑⢲⠢⠤⣄⣀⠀⠀⠀⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⠀⠀⣻⣿⢟⣽⠿⠯⠛⡸⢹⠀⢹⠒⣊⡡⠜⠓⠢⣄⠀⠀⠀⠀
⠀⠀⠀⠀⠀⠀⢀⡜⣿⣷⣽⠓⠀⢠⢂⣣⠋⠂⣾⠼⢌⠳⢄⢀⡠⠜⣣⡀⠀⠀
⠀⠀⠀⠀⠀⢠⢻⢱⣭⠷⠤⢅⠴⣡⡻⠃⠀⢠⠁⠀⢀⡱⠜⠍⢔⠊⠀⠹⡄⠀
⠀⠀⠀⠀⢀⣷⠌⠚⠷⠆⠠⠶⠭⢒⣁⠀⣤⠃⣀⢔⢋⡤⠊⠑⣄⠳⣄⠀⣧⠀
⠀⠀⠀⠀⠀⠑⠦⣀⡤⣄⠄⢄⣀⣠⣒⢦⡄⠩⠷⠦⠊⠀⠀⠀⠈⠣⡏⠢⣿⠀
⠀⠀⠀⠀⠀⠀⣸⢫⠟⣝⠞⣼⢲⡞⣞⠋⠋⠉⠋⠓⡄⠀⠀⠀⠀⠀⣨⠂⢸⡅
⠀⠀⠀⠀⠀⣰⠃⡨⠊⢀⡠⡌⢘⢇⠞⠀⠀⠀⠀⠂⠡⡄⠀⠀⢀⠞⢁⠔⢹⡇
⠀⠀⠀⠀⣰⣣⠞⢀⠔⢡⢢⠇⡘⠌⠀⠀⠀⠀⠀⠀⠠⡌⠢⡔⢁⡴⠁⠀⢸⠃
⠀⠀⠀⢠⠟⠁⠠⢊⠔⣡⢸⠀⠃⠁⠀⠀⠀⠀⠀⠀⠀⣯⠂⡀⢪⡀⠀⠀⢸⠀
⠀⢀⠔⣁⠐⠨⠀⠀⠈⠀⢄⠘⡀⠀⠈⢆⠀⠀⠀⠀⡠⢁⠜⠙⢦⠙⣦⠀⢸⠀
⡴⠁⠘⡁⣀⡡⠀⠀⠴⠒⠗⠋⠉⠉⡆⠀⠆⠄⠄⠘⠀⡎⠀⠀⠀⠑⢅⠑⢼⡀
⢯⣉⣓⠒⠒⠤⠤⣄⣀⣀⣀⣀⡀⠐⠁⠀⠀⠀⠒⠀⢀⡀⠀⠀⠀⠀⠀⠑⣌⣇
⠀⠈⢳⠄⠈⠀⠤⢄⣀⠀⢈⣉⡹⠯⡟⠁⠀⠀⠀⠀⢸⠀⠀⠂⠀⠀⡠⠚⣡⡿
⠀⢠⣋⣀⣀⣀⣀⠤⠭⢛⡩⠄⠒⠩⠂⢀⠄⠀⠀⠀⠈⢢⡀⠀⡠⠋⡩⠋⠀⢳
⠀⢹⠤⠬⠤⠬⠭⣉⣉⢃⠀⠀⣀⣀⠀⠁⠀⠀⠀⠀⡞⢺⡈⠋⡢⠊⠀⠀⠀⢸
⠀⠈⡆⠁⢀⠀⠀⠀⠉⠋⠉⠓⠂⠤⣀⡀⠀⠀⠀⠀⡧⠊⡠⠦⡈⠳⢄⠀⠀⠈
⠀⠀⢹⡜⠀⠁⠀⠀⠒⢤⡄⠤⠔⠶⠒⠛⠧⠀⠀⡼⡠⠊⠀⠀⠙⢦⡈⠳⡄⠀
⠀⠀⢸⠆⠀⠈⠀⠠⠀⠈⠀⠀⠀⠀⠀⠀⠀⠀⡜⢸⠀⠀⠀⠀⠀⠀⠑⢄⠈⢲
⠀⠀⢸⢀⠇⠀⠀⠀⠀⢀⠀⠀⠀⠀⠀⠀⡄⠊⢠⠃⠀⠀⠀⠀⠀⠀⠀⠈⡢⣸
⠀⠀⠈⠳⣤⣄⡀⠀⠀⠀⠈⠉⠉⠁⠒⠁⠀⠠⣏⠀⠀⠀⠀⠀⠀⢀⣔⠾⡿⠃
⠀⠀⠀⠀⠉⠙⠛⠒⠤⠤⣤⣄⣀⣀⣀⣔⣢⣀⣉⣂⣀⣀⣠⠴⠿⠛⠋
*/
