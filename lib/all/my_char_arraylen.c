/*
** EPITECH PROJECT, 2022
** my_char_arraylen.c
** File description:
** return the length of a given char array
*/

#include <stddef.h>

int my_char_arraylen(char **arr)
{
    int len = 0;
    for (len; arr[len] != NULL; len++) {
    }
    return (len);
}
